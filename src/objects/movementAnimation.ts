export class movementAnimation {
    public left: string;
    public right: string;
    public up: string;
    public down: string;

    public constructor(left: string, right: string, up: string, down: string) {
        this.left = left;
        this.right = right;
        this.up = up;
        this.down = down;
    }
}